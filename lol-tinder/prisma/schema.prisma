enum Role {
  TOP
  JUNGLE
  MID
  ADC
  SUPPORT
  FILL
}

enum Rank {
  UNRANKED
  IRON
  BRONZE
  SILVER
  GOLD
  PLATINUM
  EMERALD
  DIAMOND
  MASTER
  GRANDMASTER
  CHALLENGER
}

model User {
  id            String    @id @default(cuid())
  name          String?
  email         String?   @unique
  image         String?   // Discord avatar
  
  // Riot data
  riotId        String?   // Game Name
  riotTag       String?   // Tagline
  puuid         String?   @unique // ID Riot
  region        String    @default("EUW")
  rank          Rank      @default(UNRANKED)
  mainRole      Role      @default(FILL)
  secondaryRole Role      @default(FILL)
  
  bio           String?   @db.Text
  
  // Система свайпів
  likesSent     Like[]    @relation("LikesSent")
  likesReceived Like[]    @relation("LikesReceived")
}

model Like {
  id          String   @id @default(cuid())
  fromId      String
  toId        String
  isMatch     Boolean  @default(false)
  createdAt   DateTime @default(now())

  from        User     @relation("LikesSent", fields: [fromId], references: [id])
  to          User     @relation("LikesReceived", fields: [toId], references: [id])

  @@unique([fromId, toId])
}
